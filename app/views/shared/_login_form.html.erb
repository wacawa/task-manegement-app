<%= form_with(scope: :session, url: login_path, local: true) do |f| %>
    
  <%= f.label :email, "メールアドレス", class: "label-login" %>
  <%= f.email_field :email, class: 'form-control', disabled: logged_in? %>
  
  <%= f.label :password, "パスワード", class: "label-login" %>
  <%= f.password_field :password, class: 'form-control', disabled: logged_in? %>

  <% unless logged_in? %>              
    <%= f.label :remember_me, class: "checkbox inline checkbox-login", for: "remember-me" do %>
      <%= f.check_box :remember_me, id: "remember-me", disabled: logged_in? %>
      <span>ログイン情報を記憶する</span>
    <% end %>
  <% end %>

  <%= f.submit logged_in? ? "ログイン状態です" : "ログイン", class: "btn btn-primary btn-block btn-login", disabled: logged_in? %>
<% end %>
